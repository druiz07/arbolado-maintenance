{
  "name": "arbolado-maintenance-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Runner del loop de mantenimiento autónomo (policy engine + helpers + classifier + router + Aider invoker). Sin dependencia del producto.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec policy-engine/*.test.js",
    "test:watch": "node --test --watch --test-reporter=spec policy-engine/*.test.js"
  },
  "dependencies": {
    "semver": "^7.6.0"
  },
  "devDependencies": {}
}
